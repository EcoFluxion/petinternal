// ── Admin / publish settings ─────────────────────────────────────────
// After you deploy the Cloudflare Worker (see ADMIN-KURULUM.md), paste its
// URL here and re-deploy the site.
export const PUBLISH_ENDPOINT = "https://CHANGE-ME.workers.dev";

// Public site URL (used to build the "view post" link after publishing).
export const SITE_URL = "https://www.petinternal.com";
